rootProject.name = 'gradle_test'
include ':mode_a'

println("setting.gradle:>> " + settings)
println("setting.gradle:>> " + this)
[
        gradle,
        testName,
        testAge,
        name_dotGradle,
        age_dotGradle,
//        name_P,
].forEach { println(it) }

println(">>>>>>>>>>>>>>Settings properties>>>>>>>>>>>>>>>>>>")

[
        buildCache,
        extensions,
        gradle,
        pluginManager,
        plugins,
        rootDir,
        rootProject,
        settings,
        settingsDir,
        startParameter,
].forEach { println(it) }

println(">>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>")

// 打印rootProject
[rootProject.name,
 rootProject.path,
 rootProject.buildFile,
 rootProject.buildFileName,
 rootProject.projectDir,
 rootProject.parent,
 rootProject.children,
].forEach {
    println("rootProject: " + it)
}
//gradle_testt
///mnt/c/Users/YPL/githubDemo/gradle_test/build.gradle
//build.gradle
//[]
//:
//null
///mnt/c/Users/YPL/githubDemo/gradle_test

